
# Assignment: Pandas Data Analysis
# Name: Ibrahim Ahmed
# Course: Course Number  
# Assignment: Homework 4 Analyze a Dataset of your choice

## How to Run

1. # Open a terminal or command prompt.  
   - Windows: Press `Win + R`, type `cmd`, and hit Enter.  
   - Mac/Linux: Open Terminal from Applications or use a shortcut.

2. Navigate to your project folder (`Homework4`) where `analyze_data.py` is located:  
   ```bash
   cd path/to/Homework4
````

3. Check that Python 3 is installed**:


4. Run the script**:

   ```bash
   python analyze_data.py
   ```

   or, if using Python 3:

   ```bash
   python3 analyze_data.py
   ```


5. Ensure the CSV file is in the correct folder (`data/your_dataset.csv`). The script will not run if the CSV is missing or misplaced.
